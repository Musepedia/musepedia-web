<view class="common-message {{right ? 'right' : 'left'}} {{fullWidth ? 'full-width' : ''}}">
  <view wx:if="{{showArrow}}" class="message-arrow"></view>
  <text>{{text}}</text>
  <slot></slot>
</view>
